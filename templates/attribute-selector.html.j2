{# Seleção de atributo qualitativo #}
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Seleção de atributo</title>
</head>
<body>
    <h3>Selecione um atributo:</h3>
    <form id="attr-form" method="POST"  action="http://localhost:5000/salario-por-atributo" >
        <select id="attr-select" name="attr-select">
        {% for k, v in attr_dict.items() %}
            <option value="{{k}}" name="{{k}}">{{v}}</option>
        {% endfor %}
        </select>
        <input type="submit" value="OK">
    </form>

</body>
</html>