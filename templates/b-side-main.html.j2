<!DOCTYPE html>
<html lang="en">
<head>
    <title>Home</title>
</head>
<body>
    <h3>Selecione uma página:</h3>
    <form id="page-form" method="POST" action="{{ action_url }}">
        <select id="choose-page" name="choose-page">
        {% for k, v in pages_dict.items() %}
            <option value="{{k}}" name="{{k}}">{{v}}</option>
        {% endfor %}
        </select>
        <input type="submit" value="OK">
    </form>
</body>
</html>